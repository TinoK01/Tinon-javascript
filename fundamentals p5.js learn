<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Include p5.js library -->
    <script src="p5.js"></script>
    <link rel="stylesheet"
type="text/css" href="style.css">
    <meta charset="utf-8" />
  </head>
  <body>
    <script src="sketch.js"></script>
  </body>
</html>

function setup(){
  // Creates a canvas with 800px width
// and 600px height
  createCanvas(800, 600);
}

function setup() {
  // No canvas element will be created
// for this p5.js sketch
        noCanvas();
}

function setup(){
  // Sets background to a gray color
  background(127);
}

function setup(){
  // Sets the background color to white
  background(255);
}

function setup() {
        // Runs once at the beginning of
// the p5.js skecth
}

let beginSize;

// Initializing the canvas size,
background color and beginlSize value
function setup() {
  createCanvas(800, 600);
  background(220);
  beginSize = 5;
}

function setup(){
  // Runs once at the start of the
// program
}
function draw(){
  // Loops infinitely after setup() is
// run
}

function setup() {
  createCanvas(400, 800);
  conosle.log(width); // Logs 400 to
console
  console.log(height); // Logs 800 to
// console
}

function draw(){
  // Sets a red background color
  background(255, 0, 0);
}

function draw(){
  // Sets the fill color of the circle to
// blue
  fill(0, 0, 255);
  circle(100,  100, 25);
}

function draw(){
  // Sets stroke color of the square to
green
  stroke(0, 255, 0);
  square(50, 50, 25);
}

function draw() {
  // Draws a line of 5px thickness
        strokeWeight(5);
        line(50, 25, 50, 75);
}

function draw(){
  // Draws a circle with blue fill color
// and no stroke/outline
  noStroke();
  fill(0, 0, 255);
  circle(50, 50, 25);
}

function draw(){
  // The square will appear on top of the
// circle
  circle(100, 100, 100);
  square(50, 50, 100);
}

function draw(){
  // Draw a 5x5 grid of circles that are
// 25px apart horizontally and vertically
  for(let posX = 0; posX < 5; posX++) {
    for(let posY = 0; posY < 5; posY++) {
      circle(posX * 25, posY * 25, 10);
    }
  }
}

function setup(){
  // Runs once at the start of the
// program
}
function draw(){
  // Loops infinitely after setip() is
// run
}

function draw(){
  ellipse(xPos, 100, 100, 100);
  // Increment x position by 5 every draw
// loop
  xPos += 5;
}

function draw(){
  // Generate a random value between 0
// and width
  let randomX = random(width);
  // Generate a random value between 15
// and 100
  let randomSize = random(15, 100);

  circle(randomX, height / 2,
randomSize);
}

function draw() {
        // Animate ellipse's x postion
// to move from left to right as the
// frameCount variable increases
  ellipse(frameCount, height / 2, 100,
100);
}

function setup() {
  createCanvas(400, 400);
  // Set frame rate to 24 FPS, which will
// make the sketch run at a slower rate than
// the default 60 FPS
  frameRate(24);
}

function draw(){
  ellipse(100, yPos, 100, 100);
  // Decrement y position by 5 every draw
// loop
  yPos -= 5;
}

let xpos, ypos;

function setup(){
  // Call init() custom function inside
setup()
  init();
}

function draw() {
  // Call display() custom function
// inside draw()
  display();
}

// Define custom functions outside of the
setup() and draw() functions
function init(){
  xpos = width / 2;
  ypos = height / 2;
}

function display() {
  fill(255);
  ellipse(x, y, 50, 50);
}

function draw() {
  // 0 and 5 are passed in as arguments
  // Circle is drawn at (0, 5) coordinate
  makeCircle(0, 5);

  // 10 and 15 are passed in as arguments
  // Circle is drawn at (10, 5)
// coordinate
  makeCircle(10, 15);
}

// Custom function with parameters for x
and y positions
function makeCircke(x, y) {
  circle(x, y, 20);
}